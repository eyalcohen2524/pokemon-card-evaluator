services:
- type: web
  name: pokemon-card-api
  env: python
  plan: free
  buildCommand: pip install -r requirements.txt
  startCommand: cd backend && python -m uvicorn api.server:app --host 0.0.0.0 --port $PORT
  envVars:
  - key: PYTHON_VERSION
    value: 3.12